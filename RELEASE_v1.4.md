## ⚠ 从 v1.0 ~ v1.3 升级：先卸载再装

先双击 `卸载汉化.bat` 等它还原完，再双击 `安装汉化.bat`。
不先卸载的话安装器会报「和原版对不上」并且一个字节都不写 —— 那是安全设计，不是坏了。

## 修复内容

菜单 1083 / 1084 / 1085（委托的「击破」「交付」目标）和 1062（技能升级说明）这 4 条译文里，
`%d` 和 `%s` 的顺序被写反了。游戏用 `sprintf` 拼这几句，`%s` 会拿到一个整数，
`strlen((char*)1)` 读地址 `0x1` —— **打开「委托」必崩**。

```
错误: 击破%s %s %d 只（%d/%d）      实际参数: 1, '击败ＭＡＤ', '幼虫', 0, 1
正确: 击破 %d 只 %s %s（%d/%d）
```

v1.0 ~ v1.3 都受影响，**请务必更新**。
本次只有 `System.oqp3` 变了（其中 3 个 `strMenu.gstr` 成员重建，其余 19 个成员字节不变）。

## 下载

**`欧米伽五重奏_简体中文汉化_v1.4.zip`** —— 主补丁，装这个就够了。

**`欧米茄五重奏_PC汉化补充.zip`** —— 可选。用一个代理 `dinput8.dll` 在运行时把
exe 里写死的 45 条界面文本（设置 / 存读档 / 退出 / 教程提示）和 37 个 DLC 礼包名称说明换成中文。
**未经实机验证；它会往游戏进程内存写数据；没有源码，无法自行构建；杀软可能报警。**
详见包内 `README.txt` 与仓库 README 的对应章节 —— 不接受这些条件就别装，主补丁不依赖它。

## 校验

```
欧米伽五重奏_简体中文汉化_v1.4.zip    13,664,996 字节
MD5     49f31b4f1ebb33924e86cd83981b7959
SHA256  932b5204e722709ace725964c5e81c99f6d7ff55c52eaaa913135db6b8beec0d

欧米茄五重奏_PC汉化补充.zip               82,824 字节
MD5     0303caf9b63f9b90383eef5b9ad704df
SHA256  f8656affa7b431991a264331abcfffdab31b7e6ea238ad64e8accdcef7250668
  dinput8.dll        150,016 字节  MD5 9ea35deeeabef7334b93b0e4d88a4067
  oq_exe_dlc_zh.tsv    4,700 字节  MD5 27b88473a708bbff3c59cfbc7985e900
```

## 本次验证

- 原版 `System.bra` → 打补丁：22 个成员全部替换，逐条 MD5 一致
- 其余 88 个未改动成员与原档逐字节一致
- 重复安装幂等；卸载回滚后与原档逐字节相同
- 全部 22,606 条译文的格式符种类与顺序序列比对：0 不一致
- 补充包：未经实机验证
