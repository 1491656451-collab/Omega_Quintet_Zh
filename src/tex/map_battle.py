# -*- coding: utf-8 -*-
"""grSystemBattle.dds（战斗界面）—— 落在透明底上、检测框干净的那批标签。

压在底板/光条上的、以及和美术底图纠缠的标签，通用擦除器做不干净
（inpaint 会把底板糊掉），全部交给 build_battle2.py 逐条处理。

口径来源：strRpg/strSystem 已译的词（防御 4017 / 逃脱 4001 / 和声 4010 /
演唱会模式 4014 / 连锁技能 199 / 交换 304 / 待命 657 / 切换角色 464 / 修正 676），
追击、伤害、情报 沿用 grSystemString。

保持英文的三类：
  1. 数字、字母（A~L 连锁标号）、%＋−×./ 等符号
  2. BREAK! / BREAK / LIVE / Chain!! / Action(圆盘弧形字) —— 美术字，重画只会更难看
  3. 框高 ≤19px 的微型标签（Follow / Link / chain / Chain / HP GUARD）——
     汉字画进去是一团糊，和 grSystemString 里 INFORMATION 保持英文是同一条口径
"""

M = {
    # ---- 指令菜单：灰=未选中 / 金=选中，成对 ----
    127: "攻击",       128: "攻击",
    143: "技能",       136: "技能",
    148: "道具",       149: "道具",
    152: "防御",       153: "防御",
    162: "逃脱",       160: "逃脱",
    166: "和声",       167: "和声",
    173: "演唱会模式", 174: "演唱会模式",
    179: "更换搭档",   177: "更换搭档",
    184: "更换服装",   185: "更换服装",
    188: "交换",       189: "交换",
    192: "变更队列",   191: "变更队列",
    198: "待命",       199: "待命",

    # ---- 战斗中的弹出字 ----
    141: "击退",       # Knock Back
    142: "未命中",     # Miss
    150: "反转",       # Reverse
    151: "抵抗",       # Resist
    158: "重置",       # Reset
    159: "恢复",       # Recovery
    164: "偷取",       # Steal
    176: "双人防御",   # Paired Defense
    # 172 Pursuit 的检测框被下伸部截断了（只有 20px 高），交给 build_battle2 按手量的矩形做

    # ---- 其它平底标签 ----
    16:  "攻击修正",   # ATK Modifier
    161: "BLARE数据",  # BLARE Data
    170: "命中",       # HIT
    171: "伤害",       # DMG
    181: "开始",       # Start
    211: "连锁技能",   # Chain Skill
}


# 指令表是一竖列菜单，美术图里 12 条的左边缘都对齐在同一个 x 上 ——
# 说明游戏里就是左对齐排的。中文比英文短，居中放会整体右移，看着参差不齐。
ALIGN = {i: 'left' for i in (127, 143, 148, 152, 162, 166, 173, 179, 184, 188, 192, 198,
                             128, 136, 149, 153, 160, 167, 174, 177, 185, 189, 191, 199)}

# 指令表：**两列各自统一字号，但两列之间保持金色更大**。
# 用户明确：金色（选中）那列更大是对的，本来就该更大；要修的是
# 「同一列里 12 条大小不一」——美术图里每条的框高在 25~32(灰) / 29~39(金) 之间波动，
# 逐框算字号就会一条一个大小。
# 所以 ONE_SIZE 分成两组，每组内部取各自能容下的最大字号里的最小值。
ONE_SIZE = [
    [127, 143, 148, 152, 162, 166, 173, 179, 184, 188, 192, 198],   # 灰：未选中
    [128, 136, 149, 153, 160, 167, 174, 177, 185, 189, 191, 199],   # 金：选中
]

# build_battle2.py 单独处理的矩形，checkbleed 要放行（擦除矩形和落笔矩形的并集）
import os as _os, sys as _sys
_sys.path.insert(0, _os.path.dirname(_os.path.abspath(__file__)))
try:
    from build_battle2 import JOBS as _J
    ALLOW = [tuple(j['erase']) for j in _J] + [tuple(j['draw']) for j in _J]
except Exception:                                   # 循环 import 时退化为空
    ALLOW = []
