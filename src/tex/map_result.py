# -*- coding: utf-8 -*-
"""grSystemResult.dds（战斗结算）19 个框。"""
M = {
  # 面板内的白字：底板是平的，擦干净没问题，但采样会把抗锯齿边当成填充色
  # （采出来是暗蓝），所以这里手工给白色渐变和字号。
  # 擦除框必须完整盖住原文：实测 Proficiency Lv 的墨迹是 x648..729 / y71..88，
  # Affection Lv 是 x815..897 / y71..85。上一版起点给到 652 / 818，
  # 左边各留了一小截「P」和「A」没擦掉，游戏里看着像挡住了别的贴图。
  1: {"abs": [["熟练度", [643, 66, 735, 93]],
              ["好感度", [810, 66, 903, 91]]],
      "align": "left", "size": 15,
      "style": {"top": (255, 255, 255), "bot": (232, 234, 240),
                "outline": (26, 34, 58), "ew": 1}},
  5:  "好感度",        # Approval
  8:  "道具",          # Item
  9:  ["", "加成"],    # ★★★ Bonus ★★★（这页列的是各种加成百分比，跟 Rewards 区分开）
  # Total Bonus / Next：之前判定「擦不干净」是**错的**。放大看清楚之后发现
  #   ① 这两处英文大半个字身压在透明背景上，只有下半截压在光条上；
  #   ② 光条是纯竖直渐变 —— 同一行 x=56..460 颜色一模一样
  #      （只有 y=374/375 那条青色高光是横向渐变）。
  # 所以按行取背景色回填就能完全还原。这两条由 build_result_tb.py 单独处理
  # （要做 13 度斜体，通用渲染器做不了），M 里保持 None，由 ALLOW 放行。
  10: None,
  13: None,
  14: "奖励",          # Rewards（这一页列的是拿到的东西）
  17: "战斗",          # Battle
  18: "结算",          # Results
}

# build_result_tb.py 负责的矩形，checkbleed 要放行
ALLOW = [(56, 360, 242, 391), (140, 439, 246, 470)]   # 擦除 + 落笔两个矩形的并集
